app = "passmanager-stripe-service"
primary_region = "dfw"

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 3001
  force_https = false
  auto_stop_machines = "suspend"
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"

[env]
  PORT = "3001"
  STAGE = "production"
  # Cliente gRPC hacia Backend (flycast). Stripe no abre puerto gRPC.
  # BACKEND_GRPC_URL y GRPC_HMAC_SECRET van en secrets (no aquí).